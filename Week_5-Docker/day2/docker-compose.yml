services:
  mongo:
    image: mongo:6
    container_name: mongo-day2
    volumes:
      - mongo-data:/data/db

  server:
    build: ./server
    container_name: server-day2
    ports:
      - '5000:5000'
    env_file:
      - ./server/.env
    depends_on:
      - mongo

  client:
    build: ./client
    container_name: client-day2
    ports:
      - '3000:3000'
    depends_on:
      - server

volumes:
  mongo-data:
